name: Deploy Django Application

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r smartbuilding/requirements.txt

      - name: Run Tests
        run: |
          cd smartbuilding
          python manage.py test

      - name: Collect Static Files
        run: |
          cd smartbuilding
          python manage.py collectstatic --noinput

      # Deployment steps - Uncomment and configure based on deployment target

      # For Heroku deployment:
      # - name: Deploy to Heroku
      #   uses: akhileshns/heroku-deploy@v3.13.15
      #   with:
      #     heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
      #     heroku_app_name: "your-app-name"
      #     heroku_email: "your-email@example.com"

      # For AWS deployment with SSH:
      # - name: Deploy to AWS EC2
      #   uses: easingthemes/ssh-deploy@main
      #   with:
      #     SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY }}
      #     REMOTE_HOST: ${{ secrets.EC2_HOST }}
      #     REMOTE_USER: ${{ secrets.EC2_USERNAME }}
      #     TARGET: /path/to/your/app

      # For DigitalOcean App Platform:
      # - name: Deploy to DigitalOcean
      #   uses: digitalocean/app_action@main
      #   with:
      #     app_name: your-app-name
      #     token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      # For custom server deployment via SSH:
      # - name: Deploy to Server
      #   uses: appleboy/ssh-action@master
      #   with:
      #     host: ${{ secrets.HOST }}
      #     username: ${{ secrets.USERNAME }}
      #     key: ${{ secrets.SSH_PRIVATE_KEY }}
      #     script: |
      #       cd /path/to/your/app
      #       git pull origin main
      #       source venv/bin/activate
      #       pip install -r smartbuilding/requirements.txt
      #       cd smartbuilding
      #       python manage.py migrate
      #       python manage.py collectstatic --noinput
      #       sudo systemctl restart gunicorn

      - name: Deployment Complete
        run: |
          echo "Deployment workflow completed."
          echo "Configure deployment steps based on your hosting platform."
